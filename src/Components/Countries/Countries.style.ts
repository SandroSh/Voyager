import styled from "styled-components";

export const CountryItemsContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-evenly;
`;

export const UpperContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-evenly;
  margin:70px 0;
`;


export const UpperImage = styled.img`
  position:relative;
`;